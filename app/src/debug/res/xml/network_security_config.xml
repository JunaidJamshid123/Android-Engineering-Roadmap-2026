<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Debug-only override: allow cleartext to the Android emulator loopback
      address and localhost so the dev backend (http://10.0.2.2:5000) keeps
      working. This file is NOT shipped in release builds — release uses
      the manifest's android:usesCleartextTraffic="false" + the main
      network_security_config which blocks all cleartext.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
